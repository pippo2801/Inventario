/*
 * Firebase temporaneamente disattivato.
 * Il file originale è salvato in firebase.ts.backup
 *
 * Verrà riattivato quando sarà disponibile la configurazione
 * Firebase reale dello Studio Ottico Di Pietro.
 */

export const dbFirestore = null as any;
